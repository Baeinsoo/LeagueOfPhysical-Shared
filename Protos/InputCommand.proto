syntax = "proto3";

message InputCommand {
  int64 sequence_number = 1;
  float horizontal = 2;
  float vertical = 3;
  bool jump = 4;
  int32 ability_id = 6;
  // 자세 축 — 0이면 대자, 1이면 다이브. 사이는 연속이다.
  float posture = 7;
  // 패러세일을 펴고 있나. 자세 축과 무관한 별개 도구다.
  bool glide = 8;
}